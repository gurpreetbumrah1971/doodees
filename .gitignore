# Raw/working material not part of the deployed site (root-only - leading
# slash so this doesn't also match order-booking-system/assets/, which is
# the real site's CSS/JS/images and must be tracked).
/food photos/
/assets/

# AI tooling config, not site code
/.agents/
/.claude/
/skills-lock.json
